bodi-school-website/
├── app/
│   ├── about/
│   ├── academics/
│   ├── admissions/
│   ├── student-life/
│   ├── gallery/
│   ├── news/
│   ├── staff/
│   ├── contact/
│   ├── portals/
│   ├── layout.tsx
│   └── page.tsx (Home)
├── components/
│   ├── Navbar.tsx
│   ├── Footer.tsx
│   ├── Hero.tsx
│   ├── Card.tsx
│   ├── Testimonial.tsx
│   ├── Form.tsx
│   ├── SEO.tsx
│   └── ...
├── public/
│   ├── logo.png
│   └── placeholders/
├── styles/
│   ├── globals.css
│   └── tailwind.config.js
├── utils/
│   ├── seo.ts
│   └── data.ts
├── package.json
├── tsconfig.json
├── README.md
import './globals.css';
import Navbar from '../components/Navbar';
import Footer from '../components/Footer';

export const metadata = {
  title: 'Bodi Sweet Jesus School Complex',
  description: 'Education is the Key to Success',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className="scroll-smooth">
      <body className="bg-white dark:bg-gray-900 text-gray-900 dark:text-gray-100">
        <Navbar />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  );import Hero from '../components/Hero';
import Card from '../components/Card';
import Testimonial from '../components/Testimonial';

export default function Home() {
  return (
    <>
      <Hero />
      <section className="py-16 px-6 grid gap-8 md:grid-cols-3">
        <Card title="Academic Excellence" description="Shaping future leaders through rigorous programs." />
        <Card title="Community & Faith" description="Building values of discipline, innovation, and service." />
        <Card title="Global Standards" description="Competing with the best international schools worldwide." />
      </section>
      <Testimonial />
    </>
  );
}
}
'use client';
import { motion } from 'framer-motion';

export default function Hero() {
  return (
    <section className="h-screen flex flex-col justify-center items-center bg-gradient-to-r from-purple-700 to-purple-900 text-white text-center">
      <motion.img
        src="/logo.png"
        alt="School Logo"
        className="w-32 mb-6"
        initial={{ opacity: 0, y: -20 }}
        animate={{ opacity: 1, y: 0 }}
        transition={{ duration: 1 }}
      />
      <motion.h1
        className="text-4xl md:text-6xl font-bold mb-4"
        initial={{ opacity: 0 }}
        animate={{ opacity: 1 }}
        transition={{ delay: 0.5 }}
      >
        Shaping Future Leaders Through Excellence in Education
      </motion.h1>
      <div className="flex gap-4 mt-6">
        <button className="px-6 py-3 bg-gold text-black rounded-lg shadow-lg hover:scale-105 transition">Apply Now</button>
        <button className="px-6 py-3 bg-white text-purple-700 rounded-lg shadow-lg hover:scale-105 transition">Contact Us</button>
        <button className="px-6 py-3 bg-emerald-600 text-white rounded-lg shadow-lg hover:scale-105 transition">Explore Programs</button>
      </div>
    </section>
    # Bodi Sweet Jesus School Complex Website

## Setup
1. Clone repo
2. Install dependencies: `npm install`
3. Run dev server: `npm run dev`
4. Deploy: Push to GitHub → Connect to Vercel

## Features
- Next.js 15 + TypeScript
- Tailwind CSS + Framer Motion
- SEO optimized
- Dark mode
- Responsive design
- CMS-ready architecture
    
  );
}
